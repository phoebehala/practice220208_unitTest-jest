function cloneArray(array){
    return [...array]; // Spread Operator: create a brand new array with the same value but it stores in different memery address 
}                      

module.exports = cloneArray
